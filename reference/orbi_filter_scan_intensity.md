# Function replaced by `orbi_flag_outliers()`

Function replaced by
[`orbi_flag_outliers()`](https://isoorbi.isoverse.org/reference/orbi_flag_outliers.md)

## Usage

``` r
orbi_filter_scan_intensity(..., outlier_percent)
```

## Arguments

- ...:

  parameters passed on to the new function
  [`orbi_flag_outliers()`](https://isoorbi.isoverse.org/reference/orbi_flag_outliers.md).

- outlier_percent:

  outlier_percent needs to be between 0 and 10, flags extreme scans
  based on TIC x injection time (i.e., ion intensity)
